import { useAppDispatch, useAppSelector } from "./hooks";

import { setCustomModal, enableCustomModal } from "./slices";

import { store } from "./store";

export {
  setCustomModal,
  enableCustomModal,
  useAppDispatch,
  useAppSelector,
  store,
};
